<view class="page">
  <view class="card">
    <view class="section-title"><text class="section-title__main">地址管理</text><text class="section-title__sub" bindtap="addAddress">新增地址</text></view>
    <view class="address-list">
      <view class="address-item" wx:for="{{addressList}}" wx:key="addressId" bindtap="selectAddress" data-id="{{item.addressId}}">
        <view class="address-item__head"><text class="address-item__name">{{item.receiverName}}</text><text wx:if="{{item.isDefault === '1'}}" class="chip chip--warm">默认</text></view>
        <text class="address-item__phone">{{item.receiverPhone}}</text><text class="address-item__detail">{{item.provinceName}}{{item.cityName}}{{item.districtName}}{{item.detailAddress}}</text><view class="address-item__actions"><text wx:if="{{item.isDefault !== '1'}}" class="section-title__sub" catchtap="setDefault" data-id="{{item.addressId}}">设为默认</text><text class="section-title__sub" catchtap="editAddress" data-id="{{item.addressId}}">编辑</text><text class="section-title__sub" catchtap="deleteAddress" data-id="{{item.addressId}}">删除</text></view>
      </view>
    </view>
  </view>
</view>
