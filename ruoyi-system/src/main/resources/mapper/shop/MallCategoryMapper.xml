<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.shop.MallCategoryMapper">
    <resultMap id="MallCategoryResult" type="com.ruoyi.system.domain.shop.MallCategory">
        <id property="categoryId" column="category_id"/><result property="parentId" column="parent_id"/>
        <result property="categoryName" column="category_name"/><result property="level" column="level"/>
        <result property="sortNum" column="sort_num"/><result property="status" column="status"/>
        <result property="createBy" column="create_by"/><result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/><result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <sql id="categoryColumns">category_id, parent_id, category_name, level, sort_num, status, create_by, create_time, update_by, update_time, remark</sql>
    <select id="selectCategoryList" parameterType="com.ruoyi.system.domain.shop.MallCategory" resultMap="MallCategoryResult">
        SELECT <include refid="categoryColumns"/> FROM mall_category
        <where>del_flag = '0'
            <if test="categoryName != null and categoryName != ''">AND category_name LIKE concat('%', #{categoryName}, '%')</if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY parent_id IS NOT NULL, parent_id, sort_num, category_id
    </select>
    <select id="selectCategoryById" resultMap="MallCategoryResult">SELECT <include refid="categoryColumns"/> FROM mall_category WHERE category_id = #{categoryId} AND del_flag = '0'</select>
    <select id="countChildren" resultType="int">SELECT COUNT(1) FROM mall_category WHERE parent_id = #{categoryId} AND del_flag = '0'</select>
    <select id="countProducts" resultType="int">SELECT COUNT(1) FROM mall_product WHERE category_id = #{categoryId} AND del_flag = '0'</select>
    <insert id="insertCategory" parameterType="com.ruoyi.system.domain.shop.MallCategory" useGeneratedKeys="true" keyProperty="categoryId">
        INSERT INTO mall_category (parent_id, category_name, level, sort_num, status, create_by, remark)
        VALUES (#{parentId}, #{categoryName}, #{level}, #{sortNum}, #{status}, #{createBy}, #{remark})
    </insert>
    <update id="updateCategory" parameterType="com.ruoyi.system.domain.shop.MallCategory">
        UPDATE mall_category SET parent_id=#{parentId}, category_name=#{categoryName}, level=#{level}, sort_num=#{sortNum}, status=#{status}, update_by=#{updateBy}, remark=#{remark} WHERE category_id=#{categoryId} AND del_flag='0'
    </update>
    <update id="deleteCategoryById">UPDATE mall_category SET del_flag='2' WHERE category_id=#{categoryId} AND del_flag='0'</update>
</mapper>
