<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.shop.MallProductMapper">
  <resultMap id="ProductResult" type="com.ruoyi.system.domain.shop.MallProduct"><id property="productId" column="product_id"/><result property="categoryId" column="category_id"/><result property="productName" column="product_name"/><result property="subtitle" column="subtitle"/><result property="mainImage" column="main_image"/><result property="imagesJson" column="images_json"/><result property="detailHtml" column="detail_html"/><result property="productParamsJson" column="product_params_json"/><result property="specSchemaJson" column="spec_schema_json"/><result property="status" column="status"/><result property="version" column="version"/><result property="createBy" column="create_by"/><result property="createTime" column="create_time"/><result property="updateBy" column="update_by"/><result property="updateTime" column="update_time"/><result property="remark" column="remark"/></resultMap>
  <resultMap id="SkuResult" type="com.ruoyi.system.domain.shop.MallProductSku"><id property="skuId" column="sku_id"/><result property="productId" column="product_id"/><result property="skuCode" column="sku_code"/><result property="skuName" column="sku_name"/><result property="specValuesJson" column="spec_values_json"/><result property="imageUrl" column="image_url"/><result property="salePrice" column="sale_price"/><result property="marketPrice" column="market_price"/><result property="availableStock" column="available_stock"/><result property="lockedStock" column="locked_stock"/><result property="warningStock" column="warning_stock"/><result property="status" column="status"/><result property="version" column="version"/></resultMap>
  <sql id="productColumns">product_id, category_id, product_name, subtitle, main_image, images_json, detail_html, product_params_json, spec_schema_json, status, version, create_by, create_time, update_by, update_time, remark</sql>
  <select id="selectProductList" parameterType="com.ruoyi.system.domain.shop.MallProduct" resultMap="ProductResult">SELECT <include refid="productColumns"/> FROM mall_product <where>del_flag='0'<if test="categoryId != null"> AND category_id=#{categoryId}</if><if test="productName != null and productName != ''"> AND product_name LIKE concat('%', #{productName}, '%')</if><if test="status != null and status != ''"> AND status=#{status}</if></where> ORDER BY product_id DESC</select>
  <select id="selectProductById" resultMap="ProductResult">SELECT <include refid="productColumns"/> FROM mall_product WHERE product_id=#{productId} AND del_flag='0'</select>
  <select id="selectSkusByProductId" resultMap="SkuResult">SELECT sku_id, product_id, sku_code, sku_name, spec_values_json, image_url, sale_price, market_price, available_stock, locked_stock, warning_stock, status, version FROM mall_product_sku WHERE product_id=#{productId} AND del_flag='0' ORDER BY sku_id</select>
  <insert id="insertProduct" parameterType="com.ruoyi.system.domain.shop.MallProduct" useGeneratedKeys="true" keyProperty="productId">INSERT INTO mall_product (category_id, product_name, subtitle, main_image, images_json, detail_html, product_params_json, spec_schema_json, status, create_by, remark) VALUES (#{categoryId},#{productName},#{subtitle},#{mainImage},#{imagesJson},#{detailHtml},#{productParamsJson},#{specSchemaJson},#{status},#{createBy},#{remark})</insert>
  <update id="updateProduct" parameterType="com.ruoyi.system.domain.shop.MallProduct">UPDATE mall_product SET category_id=#{categoryId}, product_name=#{productName}, subtitle=#{subtitle}, main_image=#{mainImage}, images_json=#{imagesJson}, detail_html=#{detailHtml}, product_params_json=#{productParamsJson}, spec_schema_json=#{specSchemaJson}, update_by=#{updateBy}, remark=#{remark}, version=version+1 WHERE product_id=#{productId} AND del_flag='0'</update>
  <update id="deleteProductById">UPDATE mall_product SET del_flag='2' WHERE product_id=#{productId} AND del_flag='0'</update>
  <insert id="insertSku" parameterType="com.ruoyi.system.domain.shop.MallProductSku">INSERT INTO mall_product_sku (product_id, sku_code, sku_name, spec_values_json, image_url, sale_price, market_price, available_stock, locked_stock, warning_stock, status) VALUES (#{productId},#{skuCode},#{skuName},#{specValuesJson},#{imageUrl},#{salePrice},#{marketPrice},0,0,#{warningStock},#{status})</insert>
  <update id="updateSku" parameterType="com.ruoyi.system.domain.shop.MallProductSku">UPDATE mall_product_sku SET sku_code=#{skuCode}, sku_name=#{skuName}, spec_values_json=#{specValuesJson}, image_url=#{imageUrl}, sale_price=#{salePrice}, market_price=#{marketPrice}, warning_stock=#{warningStock}, status=#{status}, version=version+1 WHERE sku_id=#{skuId} AND product_id=#{productId} AND del_flag='0'</update>
  <select id="countPublishableSkus" resultType="int">SELECT COUNT(1) FROM mall_product_sku WHERE product_id=#{productId} AND del_flag='0' AND status='0' AND available_stock &gt; 0</select>
  <update id="updateProductStatus" parameterType="com.ruoyi.system.domain.shop.MallProduct">UPDATE mall_product SET status=#{status}, publish_time=CASE WHEN #{status}='1' THEN CURRENT_TIMESTAMP(3) ELSE publish_time END, update_by=#{updateBy}, version=version+1 WHERE product_id=#{productId} AND del_flag='0'</update>
  <select id="countProductsByCategoryId" resultType="int">SELECT COUNT(1) FROM mall_product WHERE category_id=#{categoryId} AND del_flag='0'</select>
</mapper>
